A binary Heap is a **semi-ordered** binary tree with last elements on the left with:

- MaxHeap: each parent is greater than both children
- MinHeap (natural): each parent is smaller than both children



