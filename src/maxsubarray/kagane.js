let arrays = [1, 2, -3, -4, 2, 7, -2, 3];

function kagane(array){
    const length =array.length;
    if (length === 0) {
        return 0;
    }

    if (length === 1) {
        return array[0];
    }



}